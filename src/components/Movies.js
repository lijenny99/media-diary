import React from 'react';
import MovieForm from '../containers/MovieForm';

const Movies = (props) => {
    return(
        <div>
            <h1>Movies</h1>
            <MovieForm />
        </div>
    )

 };

export default Movies;